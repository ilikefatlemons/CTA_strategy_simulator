---
名称: 位-指标滚动口径
模块位: 指标滚动口径
层: 执行与数据
类型: SPEC
版本: v4.0
tags: [模块位, 模块位/指标滚动口径]
---

# 位-指标滚动口径

## 职责

MA / ATR / MACD 这些滚动指标，**跨不跨时段边界**。

## 输出契约

每个周期上每个指标的逐根取值，以及**从第几根起可信**。

## 当前选中

[[指标滚动口径-连续滚动不分段重置]]

## 本位候选

- [[指标滚动口径-按时段重置带暖机带]] —— 每个时段重新起算，配一条「本时段前 N 根不发信号」的闸门。夜盘的均线不再借用白盘记忆，代价是每个时段开头都有一段盲区
- [[指标滚动口径-按交易日重置]] —— 按 `trading_date` 重置，夜盘和紧随的白盘共用一段记忆

## 改动点已经写好了

这是决策 4，**刻意的简化，不是疏漏**。`src/strategy/pullback.py` 的模块 docstring（第 21–37 行）连改哪两处都写明了：

1. 这个文件里的 `ma_*_states` / `structure_break_flags` 改成按 `session_id` 分组滚动
2. `src/engine/pullback_v3_backtest.py` 里加一条「本时段前 N 根不入场」的闸门

配置类里也早就留好了具名开关的位置。
