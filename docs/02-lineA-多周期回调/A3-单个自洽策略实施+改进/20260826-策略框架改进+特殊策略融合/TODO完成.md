# lineA-03 二稿施工单 —— 按文档顺序逐条 TODO

> **这是活文档。** 每做完一条 TODO，就回来更新下面那张进度表（状态 + commit + 实测结果），
> 所以「哪些做完了、结果是什么」永远在这一个地方。
>
> 同目录三份配套：`骨架改进+大bar特殊设计自洽.md`（二稿，TODO 的出处）、
> `基线.md` + `基线-逐笔.csv`（P0 冻下来的对照组）、`会议白板.jpg`。

## 进度

| # | TODO | 出处 | 状态 | commit / 结果 |
|---|---|---|---|---|
| — | 基线固化 | — | ✅ 完成 | `8287e18` `ec5e13b`；逐笔 CSV `387dfe1924c7e39d` |
| — | `obsidian/` 退出落地流程 | — | ✅ 完成 | `35429c1`；顺带修好五处断掉的 `goal.md` 路径 |
| **1** | 前端六个 tf + 页面切换 | L13 | ✅ 完成 | 两页各四格；511 passed / 6 skipped；逐笔 CSV 未变 |
| 2 | 驱动时钟 15m → 1m | L15 | ✅ 完成 | 入场逐笔未变；出场中位提前 10 分钟。**顺带修掉闸门用错字段** |
| 3 | 5m 抢跑 | L34 | ⏸ **高阶任务** | 跳过。先把基础链路做正确、跑通 |
| 4 | 两个开关的定名与量化定义 | L39/41/42 | ✅ 完成 | 零行为改动，逐笔 CSV `b6c55122563e066f` 前后一致 |
| 5 | 阻力位 SL | L51 | ⏸ **高阶任务** | 跳过。会改动 `R`，动它之前基础链路要先稳 |
| 6 | 吊灯路径只用 `bar[i-1]`（在 1m 时钟下复核） | L55 | ⬜ 未开始 | 存档，做法待定 |
| 7 | 吊灯 ATR 随动 | L56 | ⬜ 未开始 | 存档，做法待定 |
| 8 | 吊灯阈值，不能 TP 还亏损 | L57 | ⬜ 未开始 | 存档，做法待定 |
| 9 | 大bar 融入基本链路 | L62-68 | ⏸ **高阶任务** | 跳过。方案 B 已设计完，留着等基础链路跑通 |

## Context

规格是 `docs/02-lineA-多周期回调/A3-单个自洽策略实施+改进/20260826-策略框架改进+特殊策略融合/骨架改进+大bar特殊设计自洽.md`（二稿），代码是 `strategy/lineA_03.py` + `engine/lineA_03_backtest.py` + `performance/lineA_03_stats.py` + `viz/chart_lineA_03.py` + `run_lineA_03.py`。

**工作流（2026-08-27 改）：按二稿文件里 `>>` 出现的顺序，一条 TODO 一条 TODO 地做。**
不再按我原先那套依赖推导出的 Phase 分组。每条 TODO 是一个工作单元，做完停下来交给你核验，通过了再开下一条。

好消息是文档顺序基本上是依赖自洽的：TODO 3（5m 抢跑）排在 TODO 2（1m 驱动）之后——抢跑本来就需要亚 15m 时钟；TODO 5（阻力位 SL）排在 TODO 7/8（吊灯）之前——阻力位会缩小 `R`，而 `R` 正是吊灯几何的基准，先改 R 再定阈值才对；TODO 6（吊灯路径只用 bar[i-1]）排在 TODO 2 之后——那条不变量必须在新时钟下重新证明。**唯一逆序的是 TODO 1 排在 TODO 2 之前**，处理办法写在 TODO 1 的第 f 步。

### 已完成

| commit | 内容 |
|---|---|
| `8287e18` | 基线快照。lineA-03 六个文件首次入库 + 2h 交易日等分 + marker/十字线修复 |
| `ec5e13b` | 清掉说「1m 驱动」的过期注释；存下 P0 基线 |
| `35429c1` | `obsidian/` 定位改成研究 vault，不参与落地；修好五处断掉的 `goal.md` 路径 |

**基线（`docs/.../20260826-.../基线.md` + `基线-逐笔.csv`）**，AU · `2025-09-01 ~ 2026-07-29` · 暖机 60 天 · 成本 0bp：

```
交易 100（多55/空45）   累计净收益 +13.47%   MDD 10.46%
胜率 30.0%   盈亏比 3.05   夏普 +0.97
离场 固定止损/吊灯 51 / 49（盈30·亏19）   平均持有 32 根 15m
观测点: 回调闩锁 117 · 大周期翻转 24 · 锁1 233 · 锁2 174 · 冷静期区间 5
逐笔 CSV sha256[:16] = 387dfe1924c7e39d
```

三条要一直记着的实测：**吊灯亏损 19/49 = 38.8%**（TODO 8 的靶子）；**冷静期 214 个交易日只触发 5 次**（CD 相关的任何改动在 AU 上无法证伪）；**大周期翻转 24 次 ≈ 每 9 个交易日一次**（TODO 9 要提速的基数）。

各周期规模（本区间实测）：`1m 150,358 · 5m 30,018 · 15m 10,006 · 30m 5,140 · 2h 1,352 · 1d 274`。

---

### 施工范围

**下一条是 TODO 6。** TODO 3 / 5 / 9 跳过（高阶任务），本轮不做。 当前目标只有一个：把基础链路做**正确**、跑通。
剩下的活口是 **TODO 4 / 6 / 7 / 8** —— 全是「现有链路哪里定义得不够死」，没有一条是新增功能。 TODO 2 及之后的条目在下面留了已核实的现状与改动点作为存档，但**具体做法到时候再对**——不作为已定方案。

工作区里还留着一处 §0 的收尾：`src/run_lineA_03.py:10` 的 `goal.md` 路径修复（当时和你的在途改动混在同一个文件里没法拆开单独提交，现在那个文件已经干净了）。它跟着 TODO 1 的提交一起走。

---

## TODO 1 · 前端六个 tf + 页面切换（二稿 L13）

> `不应该只是基于四tf的策略推动，前端本身需要设计六个tf（包括进1m和1d，可以做一个页面切换的功能）`

**版面已定：两页各四格，2×2 与格高完全不变**（主图 274px / ATR 91px / MACD 91px）。

```
页1 决策层                页2 执行层
┌──────┬──────┐          ┌──────┬──────┐
│ 15m  │ 30m  │          │  1m  │  5m  │
├──────┼──────┤          ├──────┼──────┤
│  2h  │  1d  │          │ 15m  │ 30m  │
└──────┴──────┘          └──────┴──────┘
         └── 15m / 30m 两页都在，是两页的接缝 ──┘
```

**建 6 个瓦片，不是 8 个。** 15m/30m 在两页各出现一次，用「每页一个坐标」的映射表达，不复制格子——否则 1m 之外还要多推一份 15m/30m 的全部序列，十字线条目也从 24 涨到 32。

**现状与改动点**（`src/viz/chart_lineA_03.py`，行号来自当前 HEAD）：

- `:76-78` `周期集` / `GRID` / `格标签` 扩到六项。`GRID` 从 `{tf: (row,col)}` 改成 `{tf: {页: (row,col)}}`。
- `:71-72` `ROW_H=(1.0-TOP)/2`、`COL_W=0.5` **保持不变**——这是选两页方案换来的，`test_版面分数铺满屏幕:196` 的 `TOP + 2*ROW_H ≈ 1.0` 也因此不用改。
- `:120-126` `瓦片.top` / `瓦片.left` 变成按页取值。
- `:333-339` `注册瓦片` 目前**只导出 `top`**，要补 `left` 和这张每页坐标表；`window.la3` 加 `page: 1`。
- 新增 `window.la3.applyPage()`：设 `top` / `left` / `display` 并 `reSize()`。**`lwc_helpers.py:477` 的 `set_visible` 已经现成**（display + reSize，隐藏元素量出来是 0，裸改 `display` 不 reSize 会画错），`chart_lineA_03` 还没 import 它，`chart_pullback` 已经在用。
- 新 pill 组照 `建指标开关:764-810` 抄。注意 `TOP:70` 与 ticker 搜索框高度的运行时耦合（`:779` 读 `box.getBoundingClientRect()`），`chart_rbreaker.py:485-495` 有「两个容器互斥」的先例。
- `lwc_helpers.py:50` `wire_synced_crosshair` 会遍历**所有**面板，给隐藏格也发十字线。给每个 entry 加 `visible` 并跳过。
- `:944` `_限可见区间` 对零宽度的图 `setVisibleRange` 可能 no-op，页面第一次显示时要补一次。
- `src/engine/lineA_03_backtest.py:115` `计算周期` 加 `"1d"`（引擎逻辑不受影响，它只读 2h/15m/30m）。

**先例可抄**：`chart_pullback.py:102` 的 `GRID = {"1m":(0,0), "5m":(0,1), "30m":(1,0), "1d":(1,1)}` —— **1m 格和 1d 格都已经有跑通的实现**，不用从零试。

**f. 顺手把索引换算参数化（这是应付 TODO 1 排在 TODO 2 之前的办法）**
`十五到1m(基)` 只有三个内部调用点（`:411` `起箱of`、`:467` `冷静期帧`、`:574` `标记`）加一个测试引用（`test_lineA_03_chart.py:117`），另有 `推全部:624` / `推标记:658` 里硬写的 `基 = tf["15m"]`。趁这次改名成 `驱动到1m(基)` 并让 `基` 从参数来。这样 TODO 2 落地时，chart 侧只需换一个实参，不用再返工。

**要改的测试**：`:199-200` `GRID` 的键集与 2×2 坐标集断言（改成「每页的坐标集恰是那四个、每个 tf 至少出现在一页、每页恰四格」）；`:300` `4*2*2=16` → `6*2*2=24`；`:373` `len(周期集)*4` → 24；`:482` 周期→秒表加 `1m:60` / `1d:86400`。

**一个真风险**：`test_十字线按开盘对齐:392` 是 `len(周期集)²` 参数化 × 每根 bar 的 Python 循环。四格 16 组 → 六格 36 组，而且 1m 当源时单组就是 15 万次循环 × 5 个目标。**必须改成抽样，并在测试里写明抽了多少根、为什么够**（不能默默降覆盖）。

**验收**：窗口能开、能切页、两页十字线都对齐；`node --check` 那批全过；1m 格渲染耗时实测记录下来；**引擎结果一个字节都不许变**——逐笔 CSV 仍是 `387dfe1924c7e39d`。

### 完成记录（2026-08-27）

测试 **511 passed / 6 skipped**（此前 477/4）。6 个 skip 全是十字线对齐里 `tf == tf` 的对角格，正好 `len(周期集)`。**逐笔 CSV 仍是 `387dfe1924c7e39d`，引擎一个字节没变。**

做了什么：

- `计算周期` 加 `"1d"`；`ROWS/COLS` 常量化，`ROW_H`/`COL_W` 由它们推导（值不变）。
- `GRID` 从 `{tf: (row,col)}` 变成 `{tf: {页: (row,col)}}`；**只建六个瓦片**，15m/30m 换页只挪位置。
- `applyPanes` 改成按页布局：查 `t.pos[String(page)]`，本页没有的格子**三层一起藏**（只藏副图会让主图留在上一页的坑位上），并新写 `left`（原来 `left` 只由 Python 的 `pin()` 写一次，而 15m/30m 两页的列不同）。
- 新增 `applyRange`。**这一步不能省**：`_限可见区间` 是在建图那一批脚本里对所有面板发的，当时非首页的格子盒子宽度为 0，那次调用对它们是空转；不补的话切到第二页会看到全区间。
- 页面 pill 与指标 pill **共用一行一个容器**（`建指标开关` → `建顶栏开关`）。另起一行要动 `TOP`，而 `TOP` 和 pill 字号、下面四格的起始高度全都耦合着。
- `十五到1m` → `驱动到1m(基)`，`基` 从参数来。**这是给 TODO 2 铺的路**：驱动时钟换 1m 后它退化成恒等映射，三个调用点一个字不用改。

测试侧：

- `test_十字线按开盘对齐` **向量化，不是抽样**。原来的 `_首根1m` 是 `flatnonzero(bin_of == b)[0]`，**每调一次扫一遍 15 万根**；周期集从四涨到六后，1m 当源那几组会变成 ~1e11 次比较。`bin_of` 稠密非降（`_assert_shape` 保证），取相邻不等的位置即可，**覆盖与逐根循环完全相同**。
- 新增 `test_每一页都恰好铺满一个网格`（少一格留洞、重一格永久叠图）、`test_每个周期至少出现在一页上`、`test_换页的接线都在`、`test_换页会重排横向位置`。
- 时间轴同步订阅数 `4*2*2` → `len(周期集)*2*2 = 24`；偏离网格守卫补上 `1m`(必须全在网格上) 与 `1d`(必须不在)。

**实测载荷（AU，2025-09-01 ~ 2026-07-29，暖机 60 天）**：

| 格 | n_bars | 可画 | 建帧 | 载荷 |
|---|---|---|---|---|
| 1m | 150,358 | 116,998 | 0.07s | **71.5 MB** |
| 5m | 30,018 | 23,358 | 0.02s | 14.3 MB |
| 15m | 10,006 | 7,786 | 0.01s | 4.8 MB |
| 30m | 5,140 | 4,000 | 0.01s | 2.4 MB |
| 2h | 1,352 | 1,052 | 0.01s | 0.6 MB |
| 1d | 274 | 214 | 0.01s | 0.1 MB |

整窗构建（不 show）3.12s，脚本 blob **102.6 MB**（四格时代约 31 MB）。**1m 一格占了七成。**

Python 侧建帧只有 0.14s，成本全在 blob 送进 webview 那一下。同量级仓库里有先例——`run_v3_rbreaker_chart` 走的 `chart_pullback` 也在同一段数据上推 1m 格。**但这一条只能开窗实测**，headless 验不到。若实际发卡，止血办法是对 1m 格单独做「只推可见区间 + 平移时补推」（`可见交易日 = 60` 目前只限视野不限数据，初始只看得到约 26,000 根，却推了 116,998 根）。

---

---

> **以下 TODO 2 ~ 9 是存档，不是已定方案。**
> 里面的「现状」与「改动点」是已经逐行核实过的事实，可以直接用；但每条具体怎么做、
> 参数怎么定、默认值取什么，都等做到那一条时再单独对一遍。

## TODO 2 · 驱动时钟 15m → 1m（二稿 L15）

> `全部周期现在这个版本是按照15m来推动的，修改成1m推动（这样会有策略上的difference，因为后面会要求一根bar只能开一单--15m区间内只能开一单条件不符合逻辑，应采用1m）(TP/SL现在是15m推动，要改成1m)`

**先把一件事说清楚，因为它影响你怎么读这一条的结果。** 你注释里的两个理由，只有第二个会被这条 TODO 兑现：

- **「TP/SL 改成 1m 推动」——会兑现。** 止损从每 15 分钟查一次变成每分钟一次，吊灯极值按 1m 累积（线更紧）。这是这条 TODO 的全部实质。
- **「一根 bar 只能开一单」——不会因此变多。** 三条腿全在 15m 及以上，15m 信号在一个 15m 箱内是常数，所以即使换成 1m 驱动，也只有该箱的**第一根 1m** 能触发，结果仍是每 15m 箱最多一单。真正解开这条限制的是 **TODO 3（5m 抢跑）**，或者把某条腿下放到 1m。所以这条约束在 1m 驱动下几乎不 binding。

入场唯一会真的改变的类别：时段首根闸门。现在 `:277` 用 `logical_or.reduceat` 把整个 15m 箱都挡掉，1m 下只挡那一分钟。**这一类要单独报有多少笔因此改变。**

**落地形式**：`策略参数.驱动周期: str = "15m"`，`"1m"` 是新路径，两条都留着做 A/B。

**逐行改动点**（`src/engine/lineA_03_backtest.py`，已逐条核实）：

- `:229` / `:232` 时钟与 `n` 的来源；`:249-257` 驱动周期为 `1m` 时 `首` 退化成 `arange`、`closed_pos` 不再 `[首]` 取样。
- `:258-261` **因果性的机械凭据**从 `c回 == arange(n)-1` 搬到驱动周期自己的 `closed_pos`（1m 的恒等于 `i-1`，见 `v3_timeframes.py:114-135`）。**这条不能弱化**，它是「只用 bar i-1 及更早」的唯一机械证据，也是 `ValueError` 而非 `assert`（`-O` 剥不掉），保持这个形式。
- `:264-271` 价格与时间列换成驱动周期的；`:275-277` 的 `reduceat` 折叠取消；`:293-297` 五个数组；`:309` 主循环；`:428` 暖机根数（单位从 15m 根变 1m 根，`chart_lineA_03.起箱of:410` 与 `test_暖机段里一笔都不开:359` 都消费它）。
- `一笔.入场下标/出场下标/持有根数`、四类观测点、`冷静期区间` 全部换成 1m 下标；`lineA_03_stats.py:53` 与 `:158` 的 `根 15m` 与 `×15/60` 跟着驱动周期走。
- chart 侧：TODO 1 已经把 `驱动到1m(基)` 参数化了，这里只换实参；`:536-537` 的 `结果.固定止损线[基.bin_of]` gather 在 1m 下直接去掉。

**要重写的测试**：`test_入场成交价就是那根15m的open:121`（恢复「闸门把入场推到 15m 箱中间某分钟」这一类合法例外，它的 docstring 现在说这不可能）；`test_引擎不读1m价格:135`（前提整个翻转）；截断法 `:63` 的 `切15 = 脏tf["15m"].bin_of[i]` 在 1m 下就是 `i`；`_独立重算出场:204` 整个对 1m 帧重写——它的反证守卫 `max(差) > 1e-3`（`:266`）会因为 1m bar 幅度远小于 15m 而变紧，**要放宽必须写清理由，不能默默改数**。

**性能**：`n` 从 10,006 涨到 150,358（×15），主循环是纯 Python 标量循环。先测单品种耗时；超过 30s 再考虑把不涉及状态的部分预算成数组。

### 完成记录（2026-08-27）

测试 **511 passed / 6 skipped**。`驱动周期` 成了 `策略参数` 的字段，`"1m"` 是默认，`"15m"` 仍可跑；将来换逐 tick 数据只改这一个字段。

#### 一、顺带修掉一个用错字段的 bug

闸门本该挡**集合竞价撮合根**，代码用的却是 `is_session_first`（段首根）。两者在 AU 上差 274 根：

| | 根数 | 时刻 | 零振幅占比 | 成交量中位 |
|---|---|---|---|---|
| 竞价根 | 268 | 全在 21:00 | **100%** | 99 手 |
| 只是段首根 | 274 | 全在 09:01 | **0%** | 2266 手 |

09:01 是白盘第一分钟，**真实可交易**，被白挡了一年。数据里根本没有标签 `09:00` 的 bar。改用 `v3_timeframes._竞价根`（现成原语，判据是「段首根且与下一根落在不同的 30 分钟时钟格」），它的 docstring 早就写明「AU 的白盘数据里确实没有竞价根，夜盘 21:00 才有」。

#### 二、新增 `一根bar只做一个动作`

本根开的仓最早下一根才可能平。同根既开又平要求知道 bar 内部「先到开盘价还是先到止损位」的次序，而这一版的立场恰恰是**不知道**。15m 时代实测 100 笔里有 1 笔是这样成交的（2026-06-18 02:30），那一笔本质上是假的。

#### 三、三个效应分得开

| 配置 | 笔数 | 净收益 | MDD | 胜率 | 盈亏比 | 夏普 | SL:TRAIL | 吊灯盈:亏 |
|---|---|---|---|---|---|---|---|---|
| 基线（旧闸门） | 100 | +13.47% | 10.46% | 30.0% | 3.05 | +0.97 | 51:49 | 30:19 |
| 15m · 允许同根开平 | 103 | +15.60% | 10.29% | 30.1% | 3.13 | +1.10 | 50:53 | 31:22 |
| 15m · 一根一动作 | 103 | +15.41% | 10.29% | 30.1% | 3.12 | +1.09 | 50:53 | 31:22 |
| **1m · 一根一动作** | **103** | **+16.32%** | 10.44% | 30.1% | 3.17 | +1.14 | 49:54 | 31:23 |

**绝大部分变化来自闸门修正**（+2.13pp），一根一动作 −0.19pp，1m 驱动本身 +0.91pp。

#### 四、1m 驱动到底改了什么

按 1m 下标逐笔对齐 15m 与 1m 两次运行：

```
入场   103 笔完全相同，入场价最大差 0.00e+00
出场   中位提前 10 分钟，p75 14，max 27，没有一笔更晚
理由   只有 1 笔从 SL 变成 TRAIL
```

**印证了那条判断**：三条腿全在 15m 及以上，15m 信号在一个 15m 箱内是常数，所以 1m 驱动下入场仍落在该箱第一根 1m，`open` 与 15m 的 `open` 逐字节相同。1m 驱动兑现的是二稿 L15 括号里的第二条（TP/SL 改 1m 推动），**不是第一条**——要让笔数变多得靠 TODO 3。

性能：`n` 从 10,006 涨到 150,358（×15），跑时 0.1s → 0.3s。

#### 五、测试

- 截断法在 1m 下重跑通过；因果凭据从 `c回 == arange(n)-1` 搬到驱动周期自己的 `closed_pos`，形式仍是 `ValueError`（`-O` 剥不掉）。
- `test_引擎不读1m价格` → `test_引擎只读驱动周期的价格`：前提翻转，1m 之后驱动周期已是最细一层，它退化成「出场价必须真的来自那一根」。
- `_独立重算出场` 改吃驱动周期的帧，并跟着 `一根bar只做一个动作` 从 `入场下标+1` 起算。**反证阈值 `1e-3` 不用动**——我原先担心 1m bar 振幅小会让两版难分辨，实测**反了**：错排触发的是另一根 bar 上的另一个出场事件，1m 帧上价差中位 14.8、最小 6.5，比 15m 帧还大。只是非空真的笔数从 6 掉到 3。
- 两处「入场根没有线」的不对称显式钉死了（引擎侧 + 图层侧）。入场根从 `仓位` 的 0→非0 跃变推，不从 `交易` 推——末尾未平仓那一批不在 `交易` 里。

---

## TODO 3 · 5m 抢跑（二稿 L34）—— ⏸ 高阶任务，本轮跳过

> **为什么押后**：它是整张单子上唯一需要**凭空新增一个谓词**的条目（`15m即将金叉`），
> 也是过拟合风险最大的一条。基础链路还没确认正确之前做它，等于在没校准的尺子上加刻度。
> 下面的设计留着，等基础链路跑通、有可信基准之后再回来。

> `5m在此应该是要发挥作用的，用来提早判断15m的入场点。例如5m金叉&趋势上还在继续往金叉后发展，15m尚未金叉但即将金叉，则直接抢跑。`

已定：**抢跑（OR，放松入场）**，不是设计层那种第三条 AND 腿。

```
开关2 = 15m水下金叉闩锁开着  OR  (5m水下金叉闩锁开着 AND 15m即将金叉)
```

`5m水下金叉闩锁` 直接复用开关2 那个闩锁原语，喂 5m 的 MACD，零新原语。

**`15m即将金叉` 是整张单子上最大的过拟合风险点**——它是凭空新增的谓词，且「即将」天然可调。写成参数最少的形式：

```
DIF[i-1] < DEA[i-1]
且 (DEA − DIF) 连续 m 根收窄
且 (DEA − DIF)[i-1] < θ × ATR₀
```

`m` / `θ` 具名，开关 `5m抢跑` 默认关。**报告必须同时给「抢跑贡献的笔数」与「这些笔单独的收益分布」**，否则分不清它是找到了边缘还是只是在放大噪声。这也是这条 TODO 唯一能兑现「一根 bar 只能开一单」那条约束的地方。

---

## TODO 4 · 两个开关的定名与量化定义（二稿 L39 / L41 / L42）

> 2026-08-28 讨论后二稿已改写。**结论：现有实现就是这套定义，逐位相同，本条零行为改动。**

### 定下来的东西

入场的两个条件**都是开关**，区别只在记不记忆：

```
开关1  价格站回 MA21     瞬时，每根重算，不记忆
       开 = 收盘[i-1] > MA21[i-1]        关 = 不满足

开关2  MACD 水下金叉     闩锁，跨根记忆        （柱 = DIF − DEA）
       开 = 柱[i-2]<0 且 柱[i-1]>0 且 DIF[i-1]<0 且 DEA[i-1]<0
       关 = 柱[i-2]>0 且 柱[i-1]<0                （不看水位）
       空头镜像
```

**一瞬时一闩锁的不对称是承重的：**

- **开关1 必须瞬时。** 规格要的是「入场那一刻价格确实站在 MA21 之上」。闩住它就可能在价格已经跌回 MA21 之下时入场，与原意相反。
- **开关2 必须闩锁。** 金叉只在一根上成立；若也做成瞬时的，就要求它与开关1 **恰好同根**成立，概率极低，等于永不入场。

### 对拍结果：现有实现 = 这套定义

按上面的开/关条件独立写一遍闩锁，与 `lineA_03.py:194` 的 `锁2_水位金叉运行` 在 10,006 根 15m 上比对：

```
多头  3,476 根开着   逐位相同，差异 0 根
空头  3,815 根开着   逐位相同，差异 0 根
```

所以旧代码的「运行态 / 段起于水下」只是**把一个闩锁说糊了**，机制本身一直是对的。本条要做的是把措辞和命名改成上面这套，让代码读起来就是规格。

### 两个开关在实际入场时的分工（AU，103 笔）

```
                          中位   p90   本根刚成立
开关1（站回 MA21）         1 根    1      93%
开关2（水下金叉）段龄      1 根    7      39%

谁是后成立的那个（= 谁点的火）：开关1 63 笔 / 开关2 40 笔
```

**两个都在干活**，没有一个是摆设。

### 不加「段龄上限」

我原先提议给闩锁加个段龄上限（`∞` = 现状、`1` = 纯事件）。实测否掉了：

```
入场时闩锁段龄    中位 1 根   94% ≤ 8 根   最大 23 根
闩锁段本身长度    中位 14 根  最长 75 根（19 小时）
```

理论上闩锁能开着 19 小时，但入场几乎都挤在段首——因为开关1（价格站回 MA21）本身就把时点卡住了。加 8 根上限只会砍掉 1 笔。它只在设成 1~4 这种很紧的值时才起作用，那已经是**换了个策略**，不是修正确性。所以**不引入这个参数**。

### 完成记录（2026-08-28）

**零行为改动已验证**：逐笔 CSV 在改名前后都是 `b6c55122563e066f`（103 笔）。511 passed / 6 skipped。

做了什么：

- `锁2_水位金叉运行` → `动量开关`，函数体按**开 / 关**直写（`金叉 = 前柱 <= 0 < 本柱`、`死叉 = 本柱 <= 0 < 前柱`），不再绕「上行段起于水下」那套说法。用 60 组随机序列 × 4 个暖机守卫值 + 空/超短序列与旧实现对拍，**全部逐位相同**。
- `锁1/锁2` → `开关1/开关2`，`多锁/空锁` → `多开/空开`，观测点 `锁N解锁点` → `开关N点`，图层标注旗标 `锁` → `开关`，颜色 `锁N色` → `开关N色`。四个测试跟着改名。
- 两层暖机守卫原样保留。

### 实现与规格逐条核对（2026-08-28）

不止对拍，还逐笔追到**闩锁打开的那一根**上验了条件：

```
柱 == 0 的根数            全序列 10,006 根里只有 1 根（下标 0，EMA 起点）
                          且落在暖机守卫内 → `<=0` 与 `<0` 在真实数据上等价

103 笔逐笔回溯到段首      柱[开-1] 反号 · 柱[开] 同号 · 开那根 DIF/DEA 都在水下
                          · 开→R 之间没被关掉          全部成立

全序列段起点              多 222 段 / 空 252 段，三条件处处成立

关不看水位                多头 221 段里有 121 段是在 DIF/DEA 仍在水下时关掉的
                          —— 证明关确实只看柱变号
```

**平局取法记在 docstring 里**：`柱 == 0` 一律算作「不在上行」，即开写成 `柱[b-1] <= 0`、关写成 `柱[b] <= 0`。规格写的是严格 `< 0`，两者只在柱恰好为 0 时分歧，而那种情况只出现在暖机守卫内。取 `<=` 是因为「柱从 0 变正」确实是一次穿越。

---

## TODO 5 · 阻力位 SL（二稿 L51）—— ⏸ 高阶任务，本轮跳过

> **为什么押后**：它会缩小 `R = |入场价 − 固定止损|`，而 `R` 是吊灯整套几何的基准 ——
> 改了它等于同时改了 TODO 7/8 的坐标系。基础链路的止损口径还没定死之前动它，
> 后面所有以 `ATR₀` 为单位的读数都得重测一遍。

> `再加一个阻力位SL（tf用30m/2h）`

现成原语在 `src/strategy/pullback.py`：`swing_flags` / `SwingIndex` / `nearest_platform`（`:289-356`）与 `stop_level`（`:360`，ATR 与摆动平台取近再外推一个百分比），已被 `tests/test_pullback_v3_states.py:169` 逐前缀钉死。**不要另写一套。**

```
固定止损 = 取近( 入场价 ∓ 1.5×ATR₀ , 最近结构位 ∓ 缓冲 )
策略开关.止损_取结构位 ∈ {"不用", "30m", "2h"}      默认 "不用"
```

**这条会缩小 `R`，而 `R = |入场价 − 固定止损|` 是 TODO 7/8 整套吊灯几何的基准。** 文档顺序把它排在吊灯两条之前是对的：先定 R，再定阈值。做完这条要重跑一遍基线口径的分布，因为所有以 `ATR₀` 为单位的读数都平移了。

---

## TODO 6 · 吊灯路径只能用 bar[i-1]（二稿 L55）

> `吊灯TP路径问题（见同目录白板图片右上角）应该只能用bar[i-1]数据，不然依旧是未来函数`

**这一条在 15m 时钟下已经成立**：极值折入在 `lineA_03_backtest.py:396-401`，位于出场测试**之后**并带 `批 is not None` 守卫；`test_吊灯的极值严格滞后一根_不含本根的high_low:232` 用独立重算钉死，并带反证（`含本根=True` 必须给出不同答案）。

**但它排在 TODO 2 之后，所以这不是「不用做」，而是「在新时钟下重新证明一遍」。** 1m 驱动后极值按 1m 累积，同一条不变量要重新建立：`_独立重算出场` 必须对 1m 帧重写并仍然逐笔吻合，反证仍要给出不同答案。这条 TODO 的产出是一次复核 + 那条测试在 1m 下的新版本，不是新功能。

---

## TODO 7 · 吊灯 ATR 随动（二稿 L56）

> `吊灯的ATR不应该钉死在开盘的ATR，而是随着移动的价格移动（适应波动）`

**这条和一条既有不变量正面冲突，必须先讲清楚。** 引擎 `:355-364` 记着实测：实时 ATR 把 `TRAIL` 出场从 160 笔虚增到 281 笔；`test_冻结ATR让吊灯线单调:183` 钉死了「有效止损单调不降」，而这条性质**正是 ATR₀ 冻结换来的**（v3 用实时 ATR，ATR 上升而极值不变时吊灯会**后退**，`tests/test_pullback_v3_backtest.py:106` 记着）。

要同时满足 L56 和单调性，只有加**棘轮**：

```
追踪线ₜ = max( 追踪线ₜ₋₁ , 极值ₜ − 回撤 × ATRₜ )     多头；空头取 min
```

做成三值开关，默认保持现状：

```
策略开关.止盈_ATR口径 ∈ {"入场冻结", "实时", "实时带棘轮"}      默认 "入场冻结"
```

`"实时"` 是二稿的字面实现（会破坏单调性，测试要相应放开）；`"实时带棘轮"` 是唯一两者兼得的一支。三条路径各跑一次，报 `TRAIL` 笔数与收益分布的变化。

---

## TODO 8 · 吊灯阈值，不能 TP 还亏损（二稿 L57 + 白板）

> `吊灯需要加一个阈值设计，不能TP还亏损`
> 白板细化两条：打开时 `TP > SL`；`TP > Entry Price`

**先分清哪条已经满足。** 现在 `吊灯线(…, 地板=固定止损)`（`lineA_03.py:281`），几何是 `R = 1.5·ATR₀`、`固定止损 = 入场 − 1R`、`吊灯 = 极值 − 2R`：

- **`TP > SL` 已经满足**——地板就是固定止损，吊灯永不比它更差（`test_吊灯永不比固定止损更差:149`）。
- **`TP > Entry` 没有满足**——MFE 落在 `(+1R, +2R)` 时吊灯生效但坐在入场价下方（`test_吊灯的三个临界点:162` 精确钉死三个临界点）。基线实测 **19/49 = 38.8%** 的吊灯出场是亏的。

改成**先锁后追**，拆成两个具名参数：

```
止盈_解锁阈值 (默认 2.0 × ATR₀)   浮盈到多少才开始挂追踪线
止盈_追踪回撤 (默认 3.0 × ATR₀)   从极值回撤多少离场

未解锁：有效止损 = 固定止损，追踪线不存在
已解锁：有效止损 = max(固定止损, 极值 − 追踪回撤 × ATR)     解锁不可逆
```

`解锁阈值 = 0` 即现状，基线可复现。

**这不是白赚，基线里有反向证据。** 现在那批吊灯亏损单被砍在 `(−1R, 0)` 之间；基线 `SL` 的中位是 **−0.567%**，而整体 `-.5~0%` 那一桶有 32 笔、是最厚的一桶。**加了阈值它们会一路跑到 −1R 或者反弹回来**，净效果必须实测。A/B 扫 `解锁阈值 ∈ {0, 1.5, 2.0, 2.5}`。

要改测试：`:162` 与 `:269` 按解锁阈值参数化。stats 的 `吊灯亏损笔数` **保留**——阈值 >0 时它应当趋近 0，这本身就是一条断言。

---

## TODO 9 · 大bar 融入基本链路（二稿 L62-68）—— ⏸ 高阶任务，本轮跳过

> **为什么押后**：设计（方案 B）已经定完，见下；但它改的是**大周期方向的产生方式**，
> 是整条链的起点。起点还在动的时候没法判断下游改动的好坏。等基础链路跑通、
> 有可信基准之后再接。下面整节原样留着。

> `大bar的情况不应该列为一个特殊策略，而应该是一个与前面基本策略自洽的变体`

### 9a · 大bar 的量化定义（2h，读已收盘的 bar i-1）

```
空转多大阳线 ⟺ open < close  且  open < MA21 < MA55 < close
多转空大阴线 ⟺ close < open  且  open > MA21 > MA55 > close
```

加一条守卫：**大bar 方向必须与当前 `大周期方向` 相反**，否则它是顺势中继而不是反转 setup。（`MA21 < MA55` 本身就是空头均线序，多数情况自动成立，但 `大周期` 有粘性可能停在多，所以不能省。）

**已知缺口，本轮刻意不做：无尺寸门槛。** 均线纠缠时一根小 bar 也能穿过两条线而被判为大bar。你的论证是它良性——延续则不需要止损，反转则由 9d 兜住。但要**埋点**：每次 setup 记录 `|close − open| / ATR(14)_2h`，跑完报这个比值的分布与 n。尺寸门槛留到有数据后再设计（初稿 L78：「尺寸门槛是后续 filter 设计的核心」）。

### 9b · 为什么不需要任何特殊的 Entry / TP / SL

基本链路已经把两种结局都吃下了，一行新代码都不需要：

- **大bar 为假（价格掉回来）**：大周期维持原方向（空）。而大阳线把 15m 打成强多头排列 = **与 2h 完全反向 = 回调闩锁自动武装**。价格掉回来时 `开关(15m close < MA21) + 锁(水上死叉)` 成立 → 基本链路开空。初稿 L84 那条「开反向仓」的特殊规则，逐字就是基本链路自己的行为。
- **大bar 为真（价格站住）**：大周期翻多 → `:339` 清空回调武装 → 那笔潜在空单机会被取消 → 等新的多头回调。初稿 L88 那条「开同向仓」也就是基本链路在新方向上的正常入场。

**所以二稿 L70-104 整块「特殊策略」（反向仓 `TP=大bar.open` / `SL=大bar.close`、同向仓数 2 根 2h、CD 豁免）应当删掉。** 大bar 唯一需要新增的东西是：**让「为真」那一支的大周期翻转发生得更快。**

这也解掉了标尺冲突：**结构位只用来做无量纲的二元方向判定，ATR 始终且只用来度量风险**，两把尺子各管各的。

### 9c · 提速：方案 B（结构位确认）

现状为什么慢：setup 定义里 `MA21 < MA55 < close`，所以大bar 收完之后是 `close > MA55 > MA21`——而 `三线状态` 判多要求 `close > MA21 > MA55`，**均线顺序是反的**，落进「未定」，`粘住` 保持原方向。要翻多必须等 **MA21 上穿 MA55**：AU 2h 每交易日 5 根，实际交叉通常要 10~20 根，即 **2~4 个交易日**。

```
Setup 触发 → 记下 大bar.open / 大bar.close / 大bar 方向，进入「待定」
待定期间 大周期方向 一个字都不改              ← 粘性完好，下游看不见中间态
确认 ⟺ 连续 k 根 2h  close > 大bar.close      → 翻转，且此后不回退
证伪 ⟺ 2h close 跌回 大bar.open 之下（完全吞掉大bar） → setup 作废，方向不变
窗口不设固定长度：常规判据自己成立（MA21 穿过 MA55）时豁免本就无意义，自然收口
```

> **「待定期间方向不变」是被自洽性逼出来的，不是选择**：若在大bar 那根就先翻、证伪再翻回，就出现了会回退的方向，`粘住` 的语义直接破掉。初稿 L88 的「再数 2 根 2h bar」你自己标注了「没有确切证据」——这里用「证伪 + 常规判据自然收口」替掉了那个凭空的 2。
>
> 为什么用 `大bar.close` 而不是均线：setup 保证 `大bar.close > MA55 > MA21`，所以这条线**严格高于**任何均线判据，速度却一样（同为 k 根 2h ≈ 4~6 小时）。它也正是初稿 L100「反向仓 SL = 大bar.close」那个直觉的方向层重述。
>
> **「连续 k 根」是刻意复用的成语**——`冷静期_解冻根数` 已经在用「2h 上连续 3 根同方向的干净排列」。同一层用同一套写法，才叫自洽。

```
策略参数.大bar_确认方式 ∈ {"不启用", "结构位"}    默认 "不启用"（基线逐字节可复现）
策略参数.大bar_确认根数 k = 2                     A/B 扫 {1, 2, 3, 4}
```

**记录在案、本轮不实现的三条替代**（B 不够快/不够准时再上）：**A 豁免均线顺序**（连续 k 根 2h `close > max(MA21,MA55)`，同速但门槛更松，B 相对它是纯帕累托改进）；**B-快**（同一条 `大bar.close`，采样从 2h 降到 15m，`k'≈4` 根 ≈ 1 小时，**判据准确性一点没变、只是更早知道**，是 B 跑通后最自然的下一档）；**C 30m 排列确认**（复用 `三线状态`，但让 30m 一位两用；刻意不放 15m——15m 是回调层，用它定方向会和「回调 = 15m 与大周期完全反向」直接打架）。

### 9d · 大周期改变自动平仓（你 2026-08-27 指定，二稿文里没有）

现在 `:338` 明写「大周期翻转是唯一的全量重置点——清回调武装与入场状态，**但不平仓**」，于是反向持仓只能干等止损（初稿问题清单 大周期(1)）。

```
策略开关.大周期改变自动平仓: bool = True
信号：2h 的 bar i-1 收盘后判出的 大周期方向 与 当前持仓方向相反
成交：下一根 1m bar 的 open（市价单口径，不走跳空补价——没有线可跳）
出场理由：新增第三种 FLIP
```

三条要一起改：`出场理由` 从二元变三元（`test_出场理由只有两种:306` 要改，stats 面板「离场 固定止损/吊灯」变三列，**FLIP 的盈/亏也要分开报**，它和吊灯一样可盈可亏）；**CD 计数**——现有 `:392-393` 是「非 SL 出场清零连续止损」，所以 FLIP 会**静默打断 CD 连击**，语义上说得过去但确实削弱了 CD，先按现有行为走并单独报「有多少次连击被 FLIP 打断」；**依赖 TODO 2**（成交口径写的就是「下一根 1m bar」）。

### 9e · 9c 与 9d 是乘性交互

方案 B 让翻转更早更频繁，自动平仓才有戏；反过来自动平仓改变持仓分布，又影响吊灯与 CD。**必须跑 `k ∈ {1,2,3,4}` × `自动平仓 ∈ {关,开}` 的 4×2 网格，不能各测各的再合起来上。**

### 9f · 观测与图层

新增 `大bar_setup点` / `大bar_确认点` / `大bar_证伪点` 三类观测点；chart 加一个「大bar」标注 pill，在 2h 格上把大bar 那根描边、把 `大bar.open` 与 `大bar.close` 画成两条水平线直到 setup 结束。这是核对这套逻辑唯一实用的手段。

---

## 记录在案的性质（不是 bug，但会改变数字怎么读）

### ATR₀ 跨时段断口取值会很旧（2026-08-27 查实）

`固定止损 = 入场价 ∓ 1.5 × ATR(14, 30m)`，其中的 ATR 取自**成交那一刻最后一根已收盘的
30m**。已逐笔核实过：103 笔全部用这一根，0 笔用了更靠后的，0 笔存在未来函数，
`indicators.atr` 也没有尾部 shift。**这条口径是对的。**

但「最后一根已收盘的 30m」离成交时刻可以很远——因为指标连续滚动、不按时段重置
（CLAUDE.md「关键陷阱」那条），断口两侧是直接接上的：

| 成交距所用 30m 收盘 | 笔数 | 什么情况 |
|---|---|---|
| 0 分钟 | 37 | 成交正好落在 30m 收盘时刻 |
| 15 分钟 | 31 | 成交落在 30m 箱中间（15m 信号的自然结果） |
| 30 分钟 | 6 | |
| 120 分钟 | 3 | 午休 |
| 375 / 390 / 405 分钟 | 20 | **夜→日断口 6.5 小时** |
| 3270 分钟 | 2 | |
| 最大 6855 分钟 | | **4.8 天**，周末 |

**29 笔（28%）用的 ATR 比 30 分钟还旧。** 极端例：`2025-10-20 09:00 入场，用的是
10-18 02:30 的 30m` —— 周一开盘那一单，止损宽度是用上周五夜盘收盘时的波动率画的。

不是 bug（更新的 30m 数据确实不存在），但它实打实决定了开盘那批单子的 `R`。要改的话
有两条路，都不在本轮：给 ATR 加时段重置（会与「指标连续滚动」这条全仓口径冲突），
或者对跨断口的入场单独放宽/收紧倍数（等于新增一个参数）。

### 图上读不到引擎用的那根 30m

入场箭头画在**包含成交时刻**的那根 30m 上，而那根在成交时**还没收盘**。实测
103/103 笔，箭头所在的 30m 恰好比引擎所用的那根晚 1 根。所以在图上对着箭头那根读
ATR，读到的永远不是引擎用的值。**这不是引擎的问题，是图层会误导人**——已经因此
误判过一次。要修就是在 30m 格的 ATR 副图上把「引擎实际用的那根」高亮出来，
或者把冻结的 `ATR₀` 直接写进止损图例。本轮不做。

## 明确不进主线的东西

初稿 `问题清单.md` 里二稿没提的：**P3** 止损后大周期未变、回调信号同根 bar 立即重触发（`已武装` 只被大周期翻转和成交清空，止损不清空）；**P7** 出场基于 High/Low 无法防插针（TODO 2 只做度量）；**P8/P9** CD 样本量与解冻自洽性（基线实测一年 5 次，AU 上**无法证伪**，只记录）。**P2**（已持仓时大周期反转）已由 9d 解决。

`obsidian/` 不参与落地（`35429c1` 已改 CLAUDE.md），本单里凡是提到它的地方都只当先行研究引用。

---

## 贯穿的验证口径

```
python -m pytest tests/ -q          # 全绿，且不许有静默 skip
python -m src.run_lineA_03           # 逐项开关的统计面板
```

**只做 AU**，`2025-09-01 ~ 2026-07-29`，暖机 60 天。每个开关必须与基线并排输出：`笔数 n` · `多/空` · `胜率` · `盈亏比` · `累计净收益` · `最大回撤` · `夏普` · `固定止损 : 吊灯 : FLIP` · `吊灯盈 : 吊灯亏` · `平均持有` · **逐笔净收益直方图**。单一数字不算结果。

**两条纪律**：

1. **凡声称「零行为改动」的步骤，必须真的重放出逐字节相同的逐笔明细**，不能只说「应该没影响」。
   对照基准**随进度前移**：P0 是 `387dfe1924c7e39d`（100 笔）；TODO 2 之后基准是 `7e6a65c` 的
   103 笔（闸门修正 + 一根一动作 + 1m 驱动，三者都是有意的行为改动）。TODO 1 / 4 / 6 属于零行为那一类。
2. **凡新增开关，其默认值必须让当前基准可复现**。剩下的活口里会新增的是 `止盈_解锁阈值=0`、
   `止盈_ATR口径="入场冻结"`（TODO 7/8）；押后的三条各自还有 `止损_取结构位="不用"`、
   `大bar_确认方式="不启用"`、`5m抢跑=False`。全部取默认时结果必须与当前基准逐笔相同——
   这是新代码没有偷偷改变默认路径的唯一证据。（`锁2_运行段最大年龄` 已否掉，见 TODO 4。）

**样本量**：n = 100，单品种，样本外为零。这一轮至少 10 个开关 × 多个取值。`docs/02-lineA-多周期回调/Archive-美股测试/Archive-审计与修正-v2.0~v2.1/v2.1-LEDG-试验次数与过拟合折扣.md` 记着上一代「已跑过 40+ 组不同配置，样本外数据实际上为零」。**报告里要写清这一轮总共跑了多少组，最好看的那一格基本注定是噪声。**
